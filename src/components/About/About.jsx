import React from "react";

import css from "./About.module.css";
import common from "../../App.module.css";

const About = () => {
  return <div className={`${css.container} ${common.content}`}>About</div>;
};

export default About;
