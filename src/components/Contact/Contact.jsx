import React from "react";

import css from "./Contact.module.css";
import common from "../../App.module.css";

const Contact = () => {
  return <div className={`${css.container} ${common.content}`}>Contact</div>;
};

export default Contact;
